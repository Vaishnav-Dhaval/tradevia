{
  "name": "engine-service",
  "version": "1.0.0",
  "description": "Trading Engine service for 100xness trading platform",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "dev": "tsc -b && node dist/index.js",
    "dev:watch": "tsc -b --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@repo/types": "workspace:*",
    "@repo/typescript-config": "workspace:*"
  },
  "dependencies": {
    "@repo/prisma": "workspace:*",
    "@repo/redis": "workspace:*",
    "dotenv": "^17.2.2",
    "ioredis": "^5.7.0"
  }
}
